# Log-mel preprocessing (spec §4). Mirrors src/preprocess/config.py:PreprocessConfig.
# Keep in sync with the dataclass defaults.
sample_rate: 16000
clip_seconds: 10.0
mono: true

rms_gate_dbfs: -45.0

n_fft: 1024
hop_length: 320
win_length: 1024

n_mels: 128
fmin: 50.0
fmax: 8000.0

log_offset: 1.0e-6
