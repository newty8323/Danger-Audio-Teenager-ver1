"""On-device app: capture device PLAYBACK audio and run the cascade live (model_light §1).

Modules: `sources` (capture backends), `engine` (streaming cascade), `escalate` (transport to
the Linux Stage-2 host), `dashboard` (localhost UI), `server` (receiver), `main` (CLI).
"""
